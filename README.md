I have setup the frontend(react-native,tailwind) and backend (express,mongodb,prisma) setup

I want to create a video(with face-filter,mute functionality) calling and chatting(seen,lastactive update) app mobile.

techstack should be
frontend(reactnative,native-wind,zustand,jwt-auth etc)
backend(express,mongodb,prisma,(kafka or rabitMQ for bulk insertion or scalability),websocket for real time communication) i did not want to use any paid 3rd party(like stream chat api etc). I want no data sharing with other parties.